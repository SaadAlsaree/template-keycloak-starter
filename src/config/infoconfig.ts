/**
 * Infobar configuration for page-specific help content.
 *
 * Set content per page using the InfobarProvider's `content` prop.
 * This file provides a default/fallback configuration.
 */
export const infoconfig = {};
